apply plugin: 'com.android.application'

android {
    signingConfigs {
        standardselfsigned {
            keyAlias 'TheinerKey'
            keyPassword 'ngc64738'
            storeFile file('C:/Users/theinet/Keystore/android.jks')
            storePassword 'ngc64738'
        }
    }
    compileSdkVersion 19
    buildToolsVersion "20.0.0"
    defaultConfig {
        applicationId "org.theiner.quick3d"
        minSdkVersion 15
        targetSdkVersion 19
        versionCode 12
        versionName "0.8"
    }
    buildTypes {
        release {
            runProguard false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
            signingConfig signingConfigs.standardselfsigned
        }
    }
}

dependencies {
    compile fileTree(dir: 'libs', include: ['*.jar'])
}
